[package]
name = "burrow-types"
description = "Typed errors, resource limits, and shared value types for burrow."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
thiserror.workspace = true
# Wipes a password's buffer on drop with a write the optimiser may not elide. This crate
# is `forbid(unsafe_code)`, so a volatile write is not available here; zeroize exists for
# exactly this and carries no transitive dependencies of its own.
zeroize.workspace = true

[lints]
workspace = true
