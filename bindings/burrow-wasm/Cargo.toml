[package]
name = "burrow-wasm"
description = "wasm-bindgen bindings for burrow, for the web app."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
burrow-core.workspace = true

# wasm-bindgen is added in M1 with the first operation exposed to the web. Until then
# this crate proves the wasm32 target builds, which is what CI checks.
# wasm-bindgen = "0.2"

[lints]
workspace = true
